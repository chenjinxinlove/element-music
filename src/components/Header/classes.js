export default theme => ({
  container: {
    position: 'absolute',
    left: 16,
    top: 16,
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: 'calc(100vw - 32px)',
    height: 32,
    lineHeight: '32px',
    zIndex: 99
  }
});